# Insider Threat Behavioral Intelligence System — runtime dependencies
# Python 3.11+ (developed and tested on 3.13)

# --- Web layer -------------------------------------------------------------
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
flask-cors>=6.0
PyJWT>=2.8
python-dotenv>=1.0
gunicorn>=21.2          # production WSGI server

# --- Data & machine learning ----------------------------------------------
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4       # GradientBoostingClassifier, MinMaxScaler
shap>=0.44              # explainable AI

# --- Reporting -------------------------------------------------------------
reportlab>=4.0          # PDF investigation case files
openpyxl>=3.1           # Excel risk workbooks

# --- Optional --------------------------------------------------------------
# XGBoost is used automatically when importable. It needs a native OpenMP
# runtime (`brew install libomp` on macOS), so it is not a hard requirement:
# the pipeline falls back to scikit-learn Gradient Boosting.
# xgboost>=2.0
