===============================================
INSIDER THREAT DETECTION - PROJECT SETUP GUIDE
===============================================
Date: 2026-07-26
===============================================


WHAT IS THIS PROJECT?
=====================
This is an Insider Threat Detection System using the CMU CERT R6.2 dataset.
It detects employees who might steal data, hack systems, or cause harm
from inside an organization.

Dataset: ~93.3 GB of employee activity logs (email, logon, file access, etc.)
Users: 4,000 employees
Time Period: Dec 2009 - May 2011


WHAT I DID (Step by Step)
=========================

STEP 1: EXPLORED THE DATA
--------------------------
I looked at the existing files and found:
- DATA/raw/ - Original dataset (93.3 GB)
- DATA/processed/ - Already processed features
- DATA/threat_detection.db - SQLite database

I read each CSV file to understand its structure:
- http.csv (117 million rows!) - web browsing
- email.csv (11 million rows) - emails
- logon.csv (3.5 million rows) - login/logout
- file.csv (2 million rows) - file operations
- device.csv (1.5 million rows) - USB device usage
- psychometric.csv (4,001 rows) - personality scores
- LDAP/ - 18 monthly employee directory files


STEP 2: CREATED DOCUMENTATION
------------------------------
I created 4 documentation files:

1. requirements.txt
   - List of Python packages needed
   - Like a shopping list for software

2. DOCS/project_design.md
   - How the system works
   - All data source schemas
   - Feature descriptions
   - Detection methods

3. DOCS/my.md
   - Project memory
   - Key facts to remember
   - Important users (insiders)

4. DOCS/history.md
   - Timeline of what happened
   - All discoveries
   - Workflow rules (ALWAYS LOG HERE!)


STEP 3: CREATED ENVIRONMENT FILES
----------------------------------
1. .env
   - All project configuration
   - Paths, parameters, dataset info
   - NOT committed to git (secret)

2. .env.example
   - Template for new setups
   - Committed to git

3. .gitignore
   - Tells git which files to ignore
   - Excludes .env, data files, models, logs


STEP 4: CREATED DIRECTORY STRUCTURE
------------------------------------
Following your requested format:

INSIDER-THREAT-DETECTION/
├── DATA/
│   ├── raw/
│   │   ├── answers/          # Ground truth
│   │   └── CERT_R6.2/
│   │       └── data/         # All CSV files
│   ├── processed/            # Cleaned data
│   ├── features/             # Engineered features
│   └── threat_detection.db
├── NOTEBOOKS/                # Jupyter notebooks
├── SRC/
│   ├── preprocessing/        # Data loading
│   ├── feature_engineering/  # Feature creation
│   ├── models/               # ML models
│   ├── evaluation/           # Metrics
│   ├── dashboard/            # Visualizations
│   └── utils/                # Shared code
│       ├── config.py         # Path utilities
│       └── logger.py         # Logging setup
├── MODELS/                   # Saved models
├── OUTPUT/
│   ├── reports/
│   ├── graphs/
│   └── predictions/
├── LOGS/                     # Application logs
├── DOCS/                     # Documentation
├── tests/                    # Tests
├── app.py                    # Main application
├── train.py                  # Training script
├── requirements.txt
├── README.md
└── .gitignore


STEP 5: CREATED PLACEHOLDER FILES
-----------------------------------
1. app.py
   - Main entry point for the application
   - Skeleton code ready to fill in

2. train.py
   - Training script
   - Skeleton code ready to fill in

3. README.md
   - Project overview
   - How to install and run

4. SRC/__init__.py (and all subdirectories)
   - Makes Python recognize folders as packages
   - Empty files

5. .gitkeep files
   - Keeps empty directories in git
   - Git ignores empty folders otherwise


STEP 6: RENAMED FOLDERS
-------------------------
Changed: DATA/raw/r6.2/r6.2/
To:      DATA/raw/CERT_R6.2/data/

Updated paths in:
- .env
- SRC/utils/config.py
- DOCS/history.md


WHAT THE FILES DO
=================

.env (DO NOT COMMIT)
--------------------
Contains all configuration:
- PROJECT_ROOT=D:\OPENCODE\insider-threat-detection
- RAW_DATA_DIR=...CERT_R6.2\data
- CHUNK_SIZE=1000000
- RANDOM_STATE=42
- etc.

SRC/utils/config.py
-------------------
Python functions to load paths:
- get_project_root()  → returns project folder
- get_data_dir()      → returns DATA folder
- get_raw_data_dir()  → returns raw CSV files
- load_config()       → reads .env file

SRC/utils/logger.py
-------------------
Logging functions:
- setup_logger(name)  → creates logger with file output
- get_logger(name)    → gets existing logger

app.py
------
Main application entry point:
- Loads config
- Sets up logging
- Runs detection pipeline (TODO)

train.py
--------
Training script:
- Loads config
- Sets up logging
- Trains models (TODO)


IMPORTANT FACTS TO REMEMBER
============================

1. http.csv is 117 MILLION rows
   - Always process in chunks
   - Use Dask or Vaex for large files

2. 4 Known Insiders (R6.2)
   - ACM2278 (Scenario 1: data exfiltration)
   - CMP2946 (Scenario 2: job hunting + theft)
   - PLJ1771 (Scenario 3: keylogger attack)
   - CDE1846 (Scenario 4: unauthorized access)

3. Risk Score Thresholds
   - HIGH: >= 50 (investigate immediately)
   - MEDIUM: 30-50 (monitor closely)
   - LOW: < 30 (normal)

4. 39 Features per User
   - Logon: 14 features
   - File: 13 features
   - Email: 9 features
   - Device: 3 features

5. ALWAYS Check history.md
   - If stuck → read history.md
   - If confused → read history.md
   - If uncertain → read history.md


HOW TO DO THIS YOURSELF
=======================

1. Create folders manually:
   mkdir DATA DATA\raw DATA\processed DATA\features
   mkdir NOTEBOOKS
   mkdir SRC SRC\preprocessing SRC\feature_engineering
   mkdir SRC\models SRC\evaluation SRC\dashboard SRC\utils
   mkdir MODELS
   mkdir OUTPUT OUTPUT\reports OUTPUT\graphs OUTPUT\predictions
   mkdir LOGS
   mkdir DOCS
   mkdir tests

2. Create files:
   - Copy .env.example to .env and edit paths
   - Create app.py, train.py with your code
   - Create requirements.txt with your dependencies
   - Create .gitignore

3. Move data:
   - Put raw data in DATA/raw/CERT_R6.2/data/
   - Put answers in DATA/raw/answers/

4. Initialize git:
   git init
   git add .
   git commit -m "Initial project setup"


FILES CREATED IN THIS SESSION
==============================

1.  .env                    - Environment config
2.  .env.example            - Environment template
3.  .gitignore              - Git ignore rules
4.  README.md               - Project documentation
5.  requirements.txt        - Python dependencies
6.  app.py                  - Main application
7.  train.py                - Training script
8.  SRC/__init__.py         - Package init
9.  SRC/utils/__init__.py   - Utils package
10. SRC/utils/config.py     - Configuration utilities
11. SRC/utils/logger.py     - Logging utilities
12. SRC/preprocessing/__init__.py
13. SRC/feature_engineering/__init__.py
14. SRC/models/__init__.py
15. SRC/evaluation/__init__.py
16. SRC/dashboard/__init__.py
17. tests/__init__.py
18. .gitkeep files (in empty dirs)
19. DOCS/history.md         - Updated with all changes
20. DOCS/my.md              - Project memory
21. DOCS/project_design.md  - Architecture docs


===============================================
END OF GUIDE
===============================================


===============================================
SOLUTION PLAN - STEP 13: MODEL INTERPRETATION
Date: 2026-08-02
===============================================

WHAT IS THE TASK
=================
Complete NOTEBOOKS/13_Model_Interpretation.ipynb
("Explainable AI using SHAP").
It must explain the Random Forest predictions using
SHAP (SHapley Additive exPlanations) - both GLOBAL
(which features matter most) and LOCAL (why a specific
user is flagged as a threat).

WHY THE NOTEBOOK IS BROKEN RIGHT NOW
=====================================
The only cells with code are the imports.
`import shap` fails with:
    Numba needs NumPy 2.4 or less. Got NumPy 2.5.

ROOT CAUSE (and fix already applied)
=====================================
- SHAP uses `numba` for its fast tree-explainer path.
- numba 0.66.x requires numpy <= 2.4.
- The environment had numpy 2.5 -> conflict.
- FIXED in .venv: numpy downgraded to 2.4.6,
  numba 0.66.0, shap 0.52.0.
- VERIFIED WORKING: `import shap` now succeeds.

DATA / MODELS AVAILABLE
========================
- MODELS/random_forest_model.pkl   (best model, 47 features)
- DATA/processed/X_train.csv       (6392 x 47) - SHAP background
- DATA/processed/X_test.csv        (800  x 47) - to explain
- DATA/processed/labeled_dataset.csv (4000 users, 5 insiders)
- Insiders: ACM2278, CMP2946, PLJ1771, CDE1846 (+ 1 more)

NOTEBOOK STRUCTURE TO BUILD (CELL BY CELL)
==========================================
1. Markdown  - Title + intro (keep existing)
2. Imports   - pandas, numpy, matplotlib, joblib, Path (keep)
3. Load data - X_train (background), X_test, y_test
4. Load model- MODELS/random_forest_model.pkl
5. Explainer - shap.TreeExplainer(rf, X_train_sample)
               (tree-specific -> fast, exact SHAP)
6. SHAP values for X_test (and/or all 4000 labeled users)
7. GLOBAL interpretation:
   - shap.summary_plot (beeswarm) -> which features push
     prediction toward insider vs normal
   - shap bar plot (mean |SHAP|) -> global importance
   - Compare SHAP ranking vs Random Forest
     feature_importances_ (from notebook 12)
8. LOCAL interpretation - the key deliverable:
   - Waterfall / force plot for the 5 known insider users
   - Show exactly which behaviours (HTTP upload/download,
     removable media, after-hours device, file ops) pushed
     each insider toward threat=1
9. Dependence plot for the top feature (e.g.
   http_download_count or files_to_removable_media)
10. Markdown - Interpretation & conclusion:
   - Cross-check: do SHAP findings match the CERT red-team
     scenarios (data exfiltration, job hunting, keylogger)?
   - Imbalanced-data caveat: global SHAP on raw data may
     under-weight rare insider patterns; that is why the
     local insider plots matter most.
11. Save all plots to OUTPUT/graphs/
    (shap_summary.png, shap_bar.png, waterfall_<user>.png, ...)

EXPECTED INSIGHT (to verify)
============================
SHAP should confirm notebook 12's feature importance:
http_download_count, http_upload_count,
after_hours_device_events, files_to_removable_media,
file_write_count, files_from_removable_media ...
as the strongest drivers of insider classification.

===============================================
END OF STEP 13 PLAN
===============================================


===============================================
STEP 13 - IMPLEMENTED (2026-08-02)
===============================================
NOTE: The final notebook uses "Model Explainability"
(permutation-importance based) instead of SHAP, per the
user's requested content.

NOTEBOOK: NOTEBOOKS/13_Model_Interpretation.ipynb (106 cells:
          56 markdown + 50 code, all executed, 0 errors)

NOTEBOOK FLOW
=============
Part A/B (cells 0-14)
  Title + XAI intro | imports | load dataset (restore feature
  names) | load Random Forest | generate predictions |
  dataset info (800 test: 799 normal / 1 insider) |
  prediction distribution (all predicted normal) |
  visualization | interpretation (imbalance explanation)
Part C (cells 15-30) - FEATURE IMPORTANCE
  RF feature_importances_ sorted -> top 20 -> bar chart ->
  top 10 -> save feature_importance.csv -> key findings ->
  ranking summary (index 1..47) -> top 5 print
Part D (cells 31-45) - PERMUTATION IMPORTANCE
  permutation_importance(X_test, y_test, n_repeats=10,
  scoring="accuracy") -> perm_df -> top 20 -> bar chart ->
  comparison vs RF importance (merge on Feature) ->
  save permutation_importance.csv -> interpretation
Part E (cells 46-66) - INDIVIDUAL PREDICTION ANALYSIS
  prediction_df (X_test + actual/predicted labels +
  normal/insider probabilities) -> prediction distribution ->
  insider probability histogram -> top_risk ranking ->
  top 10 high risk -> bar chart -> model confidence (describe)
  -> confidence histogram -> low confidence predictions ->
  save prediction_analysis.csv -> interpretation
Part F (cells 67-81) - MISCLASSIFIED EMPLOYEE ANALYSIS
  misclassified (actual != predicted) -> stats (1 of 800) ->
  error rate 0.12% -> pie chart -> highest-error employees ->
  save misclassified_employees.csv -> interpretation
  (the 1 error is the insider predicted as normal = false neg)
Part G (cells 82-89) - EXPLAINABILITY SUMMARY
  top 10 RF features | top 10 permutation features |
  prediction summary (800/799/1, 99.88% acc) |
  save explainability_summary.csv | overall findings
  (6 observations) | final explainability report | conclusion
After Part G:
  Confusion matrix | classification report
Part H (cells 94-104) - SHAP (OPTIONAL, ADDED)
  NOTE: shap 0.52.0 works now (numpy 2.4.6 + numba 0.66.0),
  the old numba/NumPy conflict is fixed.
  TreeExplainer -> shap_values (800,47), expected 0.4998
  (class 1 = insider; result ndarray (n,47,2) -> slice [:, :, 1])
  beeswarm summary plot | global bar plot |
  top 10 mean |SHAP| -> save shap_importance.csv |
  waterfall for highest-risk employee (idx 350, prob 0.19) |
  SHAP interpretation
After Part H:
  Conclusion

KEY IMPLEMENTATION DETAILS
===========================
- Loads X_test.csv (800x47) + y_test.csv
- Restores real feature names from labeled_dataset.csv
  (drop user, label)
- IMPORTANT FIX: the model was trained with NUMERIC column
  names (0..46), so after renaming X_test columns we set
  random_forest.feature_names_in_ = feature_names so
  predict/predict_proba/permutation_importance still work.
- Predictions: every test employee predicted normal (800x0)

VERIFIED OUTPUTS
=================
- Top 5 features:
  1. working_days (0.1431)
  2. http_download_count (0.1238)
  3. http_upload_count (0.0977)
  4. after_hours_device_events (0.0888)
  5. files_to_removable_media (0.0803)
- Permutation Importance Calculated/Saved Successfully
- NOTE: permutation importance values are all 0.0 because
  the test set (1 insider) is always predicted "normal", so
  shuffling any feature never changes accuracy. The RF
  (Gini) importance remains the useful ranking here.
- Part E: highest insider probability = 0.19 (employee 350),
  model confidence mean 0.9989, min 0.81 -> saved to
  DATA/processed/prediction_analysis.csv
- Part F: 1 misclassified employee (the insider, false
  negative), error rate 0.12% -> saved to
  DATA/processed/misclassified_employees.csv
- Part G: prediction summary 800/799/1, accuracy 99.88%,
  final explainability report -> saved to
  DATA/processed/explainability_summary.csv
- Part H (SHAP): TreeExplainer SHAP values (800, 47),
  expected value 0.4998; top 5 mean |SHAP| match RF feature
  importance exactly (working_days 0.1102, http_download_count
  0.0397, http_upload_count 0.0378, after_hours_device_events
  0.0318, files_to_removable_media 0.0274); waterfall for
  highest-risk employee 350 -> saved to
  DATA/processed/shap_importance.csv

KERNEL ENVIRONMENT
==================
- numpy 2.4.6 / numba 0.66.0 / shap 0.52.0 (numba conflict
  fixed earlier; not used in final notebook)

===============================================
END OF STEP 13 IMPLEMENTATION
===============================================


===============================================
SOLUTION PLAN - STEP 14: STREAMLIT DEPLOYMENT
Date: 2026-08-02
===============================================

WHAT IS THE TASK
=================
Build NOTEBOOKS/14_Streamlit_Deployment.ipynb.
The notebook PREPARES a professional Insider Threat
Detection Web Application (dashboard) using Streamlit.
It creates the STREAMLIT/ project folder with the app
files, then builds them part by part:
  app.py  (main dashboard, multiple pages)
  predict.py  (prediction logic / preprocessing)
  utils.py    (helpers)
  style.css   (custom styling)
  requirements.txt
Deploys the trained Random Forest model
(MODELS/random_forest_model.pkl, 47 features).

NOTE: NOTEBOOKS/14_Model_Deployment.ipynb exists but is
an EMPTY placeholder (invalid JSON, never populated).
Notebook 14 will be created as 14_Streamlit_Deployment.ipynb
matching the user's title. Ask user before deleting the
placeholder.

KEY FACTS FOR THE APP
======================
- Model: RandomForestClassifier(class_weight='balanced',
  random_state=42), 47 features, trained with NUMERIC column
  names (0..46). The app must map input to the real feature
  order (from labeled_dataset.csv: drop user, label).
- Feature names restored from DATA/processed/labeled_dataset.csv
- 47 features order matters for predict_proba.
- Dataset: 4000 users, 5 insiders (ACM2278, CDE1846, CMP2946,
  MBG3183, PLJ1771).

PLANNED NOTEBOOK FLOW (user provides cell content per part)
============================================================
Part A  - Project structure (create STREAMLIT/ + assets/)
Part B  - Create streamlit files (app.py, predict.py, utils.py,
          style.css, requirements.txt)
Part C  - Verify model + feature names (MODELS/, 47 features)
Part D+ - Build app.py (Home / Dashboard / CSV Upload /
          Single Employee Prediction / About) +
          predict.py + utils.py + style.css + requirements.txt
          - verify app.py imports & functions work (pytest/API
            checks), optionally smoke-test with streamlit

CELLS FOR THIS BATCH (cells 0-15, 8 md + 8 code)
=================================================
md 0  "# Insider Threat Detection Web Application" intro
code 1  imports: from pathlib import Path, import os
md 2  "# Create Project Folders"
code 3  STREAMLIT_PATH.mkdir + assets -> "Folder Created Successfully"
md 4  "# Verify Folder Structure"
code 5  print exists + iterdir -> assets
md 6  "# Create Streamlit Files"
code 7  touch app.py predict.py utils.py style.css requirements.txt
md 8  "# Verify Files"
code 9  iterdir print
md 10 "# Verify Saved Models"
code 11 MODEL_PATH iterdir -> logistic_regression.pkl,
        decision_tree.pkl, random_forest_model.pkl
md 12 "# Load Random Forest Model"
code 13 joblib.load(MODEL_PATH/"random_forest_model.pkl") -> RF
md 14 "# Verify Feature Names"
code 15 labeled_dataset.csv drop user,label -> 47 features

===============================================
END OF STEP 14 PLAN
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 1
===============================================
NOTEBOOK: NOTEBOOKS/14_Streamlit_Deployment.ipynb
          (16 cells: 8 markdown + 8 code, all executed,
          0 errors)

CREATED
=======
- STREAMLIT/ folder + STREAMLIT/assets/
- Empty files: app.py, predict.py, utils.py,
  style.css, requirements.txt
- NOTEBOOKS/14_Streamlit_Deployment.ipynb
  (NOTE: old NOTEBOOKS/14_Model_Deployment.ipynb is an
  EMPTY placeholder - invalid JSON. Still present; ask
  user whether to delete.)

VERIFIED
========
- MODELS/: .gitkeep, decision_tree.pkl, logistic_regression.pkl,
  random_forest_model.pkl  (note: expected output listed only
  3 pkl files; .gitkeep also present -> benign)
- random_forest = RandomForestClassifier(class_weight='balanced',
  random_state=42)
- labeled_dataset.csv -> drop(user,label) -> 47 features
- iterdir listing is alphabetical (assets before predict.py),
  unlike the user's sample order -> benign

CELL MAP (16 cells)
===================
md 0  intro | code 1 imports (Path, os)
md 2  create folders | code 3 STREAMLIT_PATH+assets
md 4  verify structure | code 5 exists+iterdir
md 6  create files | code 7 touch 5 files
md 8  verify files | code 9 iterdir
md 10 verify models | code 11 MODEL_PATH iterdir
md 12 load RF | code 13 joblib.load
md 14 verify features | code 15 47 features

===============================================
END OF STEP 14 BATCH 1
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 2
===============================================
NOTEBOOK: NOTEBOOKS/14_Streamlit_Deployment.ipynb
          (22 cells: 11 markdown + 11 code, all executed,
          0 errors)

WHAT WAS ADDED (Part B - create app.py)
========================================
- md 16  "# Create app.py" intro
- code 17  defines app_code (triple-quoted string) = the
            full Streamlit app: page config, load model,
            load feature names, sidebar radio (Home /
            Single Prediction / Batch Prediction / About),
            and 4 page branches (Home + About mostly built,
            Single/Batch prediction are placeholders)
- md 18  "# Save app.py"
- code 19  writes app_code -> STREAMLIT/app.py
            ("app.py Created Successfully")
- md 20  "# Verify app.py"
- code 21  print((STREAMLIT_PATH/"app.py").exists()) -> True

IMPORTANT ENCODING FIX (PowerShell 5.1 gotcha)
==============================================
- Literal emoji (🛡️ U+1F6E1+FE0F, • U+2022) typed directly
  in the bash->PowerShell command get CORRUPTED before they
  reach Python (codepage mismatch).
- FIX: the cell content is passed as ASCII escape text
  (\U0001F6E1\uFE0F, \u2022) and converted in-memory by
  Python to real unicode before writing the notebook JSON.
- Result: notebook cell 17 source contains REAL emoji,
  and STREAMLIT/app.py contains correct UTF-8 emoji + bullets.
- NOTE: PowerShell Get-Content/Select-String will DISPLAY
  UTF-8 emoji as mojibake ("dY>��,?"); always verify with
  Python (io.open encoding="utf-8").

VERIFIED
========
- app.py created, size ~2.1 KB UTF-8
- py_compile STREAMLIT/app.py -> syntax OK
- page_icon="🛡️", title "🛡️ Insider Threat Detection
  System", bullets "• Python", "• Random Forest" correct
- streamlit IS installed in .venv (can smoke-test later)

STREAMLIT/ NOW CONTAINS
=======================
  app.py (2262 B -> real content), predict.py, utils.py,
  style.css, requirements.txt (all empty), assets/

===============================================
END OF STEP 14 BATCH 2
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 3
===============================================
CHANGE: STREAMLIT/app.py edited DIRECTLY (user decision:
        notebook 14 only CREATES app files; further app
        changes go straight into the .py files, NOT into
        notebook cells). NOTE: this means re-running
        notebook 14 would overwrite app.py with the older
        version (cell 17 app_code). Documented as known
        behaviour.

WHAT CHANGED (Single Prediction section)
========================================
Replaced the placeholder block with a real ML form:
- st.title("👤 Single Employee Prediction")
- 2-column grid of number_input widgets, one per feature
  (47 inputs, key=feature, value=0.0, format="%.2f")
- "Predict Insider Threat" button
- on click: build input_df (1 row x 47 cols, real feature
  names), model.predict -> prediction, predict_proba ->
  probability
- result: st.error("⚠ Insider Threat Detected") if 1 else
  st.success("✅ Normal Employee")
- probabilities shown: Normal / Insider, 4 decimals
- NOTE: predict/predict_proba are positional -> works even
  though the model was trained with numeric column names.

VERIFIED
========
- py_compile STREAMLIT/app.py -> syntax OK
- emoji present and correct UTF-8 (👤 U+1F464, ⚠ U+26A0,
  ✅ U+2705) - verified via Python io.open
- smoke test: streamlit run app.py --server.headless true
  --server.port 8502 -> Uvicorn started, "You can now view
  your Streamlit app", model loaded, no errors.
  (server killed after 25 s timeout; port confirmed free)
- run from STREAMLIT/ so ../MODELS and ../DATA resolve to
  project root

NEXT (expected)
===============
Risk level UI (🟢 LOW / 🟡 MEDIUM / 🔴 HIGH) with metric
cards, progress bars, professional dashboard styling.

===============================================
END OF STEP 14 BATCH 3
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 4
===============================================
CHANGE: STREAMLIT/app.py - Batch Prediction section
        (edited directly, per user decision).

WHAT CHANGED
============
Batch Prediction page is now functional:
- st.title("📂 Batch Prediction")
- file_uploader (type=csv)
- preview: st.dataframe(data.head())
- "Run Prediction" button
- COLUMN VALIDATION: missing = [col for col in feature_names
  if col not in data.columns]; if missing -> st.error
  ("Missing Columns: ..."); else -> run predictions
- predictions + predict_proba -> add Prediction,
  Normal Probability, Insider Probability, Risk Level
  (🟢 Low < 0.30, 🟡 Medium < 0.70, else 🔴 High)
- st.success + results dataframe + download_button CSV
- NOTE: validation + all prediction/results code is NESTED
  inside the else (user's literal replacement would have left
  predict_proba/results outside the else and crashed with
  NameError when columns are missing -> structured to actually
  work; explained to user)

CRITICAL BUG FIX (affects BOTH prediction pages)
================================================
- sklearn >=1.0 validates feature names in predict/predict_proba.
  The model was trained with NUMERIC column names (0..46), so
  predict() on a DataFrame with real feature names raised:
  "The feature names should match those that were passed during
  fit."
- The app smoke tests only START the server (predict code only
  runs on button click) so this was NOT caught by smoke tests.
  Caught by simulating the batch logic headlessly.
- FIX (same as notebook 13): after loading feature names:
      model.feature_names_in_ = np.asarray(feature_names)
  plus `import numpy as np`.
- This ALSO fixes the Single Prediction page which had the
  same latent bug.

VERIFIED
========
- py_compile -> syntax OK
- positive path: X_test.csv (renamed to real features) ->
  predict/predict_proba/risk all work, all 🟢 Low (expected,
  X_test is all normal)
- negative path: raw X_test.csv (numeric col names) ->
  validation flags ALL 47 columns missing -> st.error path
  works as designed
- emoji correct UTF-8 (📂 U+1F4C2, 🟢 U+1F7E2, 🟡 U+1F7E1,
  🔴 U+1F534). NOTE: download_button label has NO ⬇ emoji
  (mockup only - matches user code)
- streamlit run smoke test: server started, no errors

===============================================
END OF STEP 14 BATCH 4
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 5
===============================================
CHANGE: STREAMLIT/app.py - Home page turned into a
        professional Dashboard (edited directly).

WHAT CHANGED (Home section fully replaced)
==========================================
- title: "🛡 Insider Threat Detection Dashboard"
- welcome message
- 3 metric cards (Model / Features / Dataset)
- Project Overview (behavioural data categories)
- Project Workflow table (6 steps, Step 1..6)
- Model Information table (RF, 3200 train, 800 test, 47 feats)
- Dataset Distribution bar chart (Normal 3995 / Insider 5)
- Feature Categories table (6 categories, all Yes)
- Technologies Used table (6 technologies)
- footer caption (B.Tech AI & ML Major Project)

VERIFIED (streamlit.testing AppTest - headless page render)
==========================================================
- HOME: 0 exceptions; title, 3 metrics, 6 subheaders,
  4 tables, 1 vega_lite bar chart, caption all present
- SINGLE: 0 exceptions; 47 number_inputs, 1 button;
  clicking Predict with all-zero inputs -> model predicts
  INSIDER (0.51) -> st.error shown, probabilities printed
  (correct behaviour - balanced class weights push zero
  input toward minority class; app handled it fine)
- BATCH: 0 exceptions; file uploader present
- ABOUT: 0 exceptions
- streamlit run: server starts cleanly

NOTES
=====
- benign pyarrow warning: "Serialization of dataframe to
  Arrow table was unsuccessful. Applying automatic fixes..."
  from mixed-type "Value" column in model_info table
  (strings + ints). Streamlit auto-fixes; displays fine.
- AppTest gives much stronger verification than a server
  smoke test (renders pages + runs button-click code).

===============================================
END OF STEP 14 BATCH 5
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 6
===============================================
CHANGE: STREAMLIT/app.py - Part F: professional result
        cards + risk analysis (edited directly).

SINGLE PREDICTION (result section replaced + 2 additions)
=========================================================
- st.header("Prediction Result") with 2 columns:
  col1 -> 🔴 Insider Threat Detected / 🟢 Normal Employee
  col2 -> 🟢 LOW RISK (<0.30) / 🟡 MEDIUM RISK (<0.70) /
          🔴 HIGH RISK (else)
- Prediction Probability: st.progress(insider prob) +
  two metric cards (Normal/Insider Probability as %)
- Employee Summary: dataframe of Feature + Value
- Recommendation: success/warning/error by probability

BATCH PREDICTION (expanded after results table)
===============================================
- Prediction Summary: total/normal/insider + low/medium/high
- metric row 1: Employees / Normal / Insiders
- metric row 2: 🟢 Low / 🟡 Medium / 🔴 High
- Risk bar chart (Low/Medium/High)
- Prediction bar chart (Normal/Insider)
- Highest Risk Employees: top 10 by Insider Probability
- download_button now keyword args (file_name=, mime=) and
  label "⬇ Download Prediction Results"

VERIFIED (streamlit AppTest - full interaction)
===============================================
- SINGLE: 0 exceptions. Zero-input predict -> insider 0.51 ->
  error "Insider Threat Detected", warning "MEDIUM RISK" +
  "Employee should be monitored.", progress bar, metrics
  49.00%/51.00%, Employee Summary dataframe.
- BATCH: 0 exceptions. Simulated CSV upload (8 rows from
  X_test with real feature names) -> success, metrics
  (8/8/0 normal/insider; 8/0/0 low/med/high), 4 subtitles,
  2 bar charts, 1 download button, top-10 df (8x51).
- AppTest file_uploader API: set_value(("name", bytes,
  "text/csv")) (single tuple arg).
- benign pyarrow auto-fix warning still present (mixed-type
  model_info Value column) - cosmetic only.

===============================================
END OF STEP 14 BATCH 6
===============================================


===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 7
===============================================
Parts G, H, I, J.

PART G - About page replaced in app.py (direct edit)
====================================================
- title "ℹ️ About the Project"
- Project Overview (behavioural data categories)
- Machine Learning Model table (RF, 3200/800, 47, CERT R6.2)
- Project Workflow table (7 steps incl Explainable AI +
  Deployment)
- Technologies Used (7 incl Matplotlib)
- Project Highlights (6 st.success checkmarks)
- caption "Developed as a B.Tech AI & ML Major Project"

PARTS H/I/J - notebook cells added (cells 22-27)
================================================
- md 22 / code 23: write requirements.txt
  (streamlit, pandas, numpy, matplotlib, scikit-learn,
   joblib, shap) -> "requirements.txt Created Successfully"
- md 24 / code 25: write style.css
  (.main bg #f5f5f5, .stButton full-width 10px radius 45px,
   h1 #0E76A8) -> "CSS Created Successfully"
- md 26 / code 27: print run instructions
  ("Open Terminal / cd STREAMLIT / streamlit run app.py")
Notebook now 28 cells, all executed, 0 errors.

*** CRITICAL INCIDENT - NOTEBOOK OVERWRITE ***
==============================================
- Executing notebook 14 re-ran cell 19 which WRITES the
  old app_code string (cell 17) over STREAMLIT/app.py.
- This WIPED all direct edits from Parts C-G (Single,
  Batch, Home dashboard, Part F results, numpy
  feature_names_in_ fix, new About page) -> app.py reverted
  to the 117-line placeholder version.
- DETECTED by AppTest (About page showed old title).
- FIXED: app.py fully rewritten with the complete final
  version (all Parts C-G combined).
- VERIFIED: all 4 pages AppTest, 0 exceptions; single
  predict + batch CSV upload + about all working.
- RULE GOING FORWARD: DO NOT re-execute notebook 14 while
  app.py is edited directly (cell 17 app_code is stale).
  If the notebook must reproduce the app, cell 17 needs to
  be updated to the final app.py (see refactor proposal).

===============================================
STEP 14 - IMPLEMENTED (2026-08-02) - BATCH 8
===============================================
REFACTOR: monolithic app.py -> modular app + notebook
overwrite hazard removed. User-approved plan.

1) SPLIT THE APP INTO 3 MODULES + CSS
=====================================
- STREAMLIT/utils.py   (NEW)
    BASE_DIR = Path(__file__).resolve().parent
    MODEL_PATH / DATA_PATH / CSS_PATH (resolved from BASE_DIR)
    get_feature_names()   @st.cache_data
    load_model()          @st.cache_resource (sets feature_names_in_)
    load_css()            reads style.css, st.markdown unsafe_allow_html
- STREAMLIT/predict.py   (NEW)
    imports utils; exposes MODEL, FEATURE_NAMES
    risk_level(prob)      Low <0.30 / Medium <0.70 / High
    predict_single(features)  -> (prediction, probability)
    predict_batch(data)   -> df + Prediction / Normal+Insider Prob /
                            Risk Level columns
- STREAMLIT/app.py       (REWRITTEN as UI-only)
    imports utils + predict; FEATURE_NAMES from predict
    Home / Single / Batch / About unchanged in behaviour
    model_info_rows shared for Home (first 4 rows) + About (5 rows)
- STREAMLIT/style.css    now ACTUALLY loaded via utils.load_css()
- sys.path guard added to app.py + predict.py:
    sys.path.insert(0, str(Path(__file__).resolve().parent))
    -> app runs from STREAMLIT/ OR project root

2) NOTEBOOK OVERWRITE HAZARD REMOVED (user approved)
=====================================================
- Removed old cells 16-21 that held the stale monolithic
  app_code string and WRITE app.py on every run.
- Replaced with:
    md  "The app is maintained directly in the files... does
         NOT rewrite them... safe to re-run"
    code verify app.py / predict.py / utils.py / style.css /
         requirements.txt exist (no writing)
- Notebook now 24 cells; re-executed, 0 errors; app.py,
  predict.py, utils.py CONFIRMED untouched after re-run.

3) VERIFICATION
===============
- AppTest from STREAMLIT/ AND from project root:
  HOME/SINGLE/BATCH/ABOUT = 0 exceptions each; single predict
  zero-input -> insider 0.51; batch CSV 8 rows -> 8/8/0 + 8/0/0,
  2 charts, top-10 df, download button; About 5 subtitles,
  3 tables, 6 success.
- Real streamlit run smoke test: `python -m streamlit run
  app.py --server.headless true` -> HTTP 200.
- pyarrow mixed-type warning on model_info table is the benign
  auto-fix (Value column int+str).

===============================================
STEP 14 - IMPLEMENTED (2026-08-03) - BATCH 9
===============================================
OPTION 3: professional type-aware + grouped inputs
on the Single Prediction page (user-directed).

1) WIDGETS NOW MATCH ACTUAL DATA TYPES
======================================
- IMPORTANT: none of the 47 features are true 0/1 booleans
  (after_hours_events spans 0..2763 etc.), so the user's
  sample boolean_features/selectbox list does NOT fit this
  dataset. Used real dtypes instead:
    int features   -> st.number_input step=1 format="%d"
    float features -> st.number_input step=0.1 format="%.2f"
- utils.get_float_features() (@st.cache_data) reads
  labeled_dataset.csv and returns frozenset of float-dtype
  feature names (21 floats, 26 ints).
- app.py exposes FLOAT_FEATURES = utils.get_float_features().

2) INPUTS GROUPED INTO 6 CATEGORY EXPANDERS
===========================================
FEATURE_CATEGORIES (order == FEATURE_NAMES order, verified):
  📂 Logon Features         (11)
  💻 Device Features        (5)
  📧 Email Features         (7)
  📁 File Features          (11)
  🌐 HTTP Features          (8)
  🧠 Psychometric Features  (5)
Each expander = 2 columns of inputs. Input values appended in
category order, which matches FEATURE_NAMES exactly, so the
predict_single DataFrame columns stay aligned.

3) VERIFICATION
===============
- Coverage: 47 categorized, 0 missing, 0 extra, 0 dups.
- Order identical to FEATURE_NAMES: True.
- AppTest: 6 expanders rendered; 47 number_inputs (int/float
  correct); predict click 0 exceptions; zero-input ->
  insider 0.51 (49/51% metrics).
- Real streamlit run smoke test -> HTTP 200.

===============================================
END OF STEP 14 BATCH 9
===============================================


===============================================
NEXT STEPS FOR YOU (2026-08-07)
===============================================
The Streamlit dashboard now RUNS. Do these in
order. Each one is a task YOU should complete.

STEP 1: OPEN AND TEST THE DASHBOARD
=====================================
The app is already running at:
   http://localhost:8501
(or start it yourself with):
   .venv\Scripts\streamlit run STREAMLIT\app.py

Test ALL 4 pages in the sidebar:
1. Home        - title, 3 metric cards, workflow
                 table, bar chart, feature table
2. Single Prediction - expanders with 47 inputs,
                 click "Predict Insider Threat"
3. Batch Prediction  - upload a CSV, click
                 "Run Prediction", download results
4. About       - model info, workflow, highlights

Write down any page that shows an error/blank.

STEP 2: FIX WHAT YOU FIND
==========================
If a page errors, look at the Terminal window
where streamlit runs - it prints the full error.
Common issues:
- Model file missing  -> MODELS\random_forest_model.pkl
- Data file missing   -> DATA\processed\labeled_dataset.csv
- Feature mismatch    -> keep column order of
  labeled_dataset.csv minus (user, label)
Note: the root app.py is NOT the dashboard. Always
run STREAMLIT\app.py.

STEP 3: TEST BATCH WITH A REAL CSV
===================================
Batch prediction needs a CSV with all 47 feature
columns (same names/order as labeled_dataset.csv,
minus "user" and "label").
Test file already exists:
   DATA\processed\X_test.csv
Copy it to STREAMLIT\sample_data.csv and upload it
in the Batch Prediction page. All 47 columns must
be present or the app shows "Missing Columns".

STEP 4: RUN THE PROJECT TESTS
==============================
There is a tests\ folder. Run it and make sure
nothing is broken:
   .venv\Scripts\python -m pytest tests\ -v

STEP 5: COMMIT YOUR WORK TO GIT
================================
Everything is currently UNTRACKED (nothing was
ever committed). This is the first real commit:
   git add .
   git commit -m "Insider Threat Detection: full pipeline + Streamlit dashboard"
Check .gitignore first - it must exclude:
   .env, DATA\raw, MODELS\*.pkl, LOGS, OUTPUT

STEP 6: FINAL REPORT / DEMO PREP
=================================
For submission/demo, prepare:
1. A final report (use DOCS\project_design.md and
   the notebook 13 explainability results:
   top features = working_days, http_download_count,
   http_upload_count, after_hours_device_events,
   files_to_removable_media)
2. A demo script:
   - show Home page
   - Single Prediction for a known insider
     (ACM2278, CMP2946, PLJ1771, CDE1846, MBG3183)
   - Batch upload of X_test.csv -> risk summary
3. Update README.md with:
   - correct run command (streamlit run STREAMLIT\app.py)
   - the 5 known insider user IDs
   - screenshots of the dashboard

STEP 7: RECORD THIS IN history.md
=================================
Project rule: ALWAYS log what you do in
DOCS\history.md (date + what changed + result).

===============================================
END OF NEXT STEPS
===============================================
